import {createTheme} from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F0902F',
      contrastText: '#fef8f3',
    },
    secondary: {
      main: '#575656',
      contrastText: '#fef8f3',
    },
  },
});

export default theme;
